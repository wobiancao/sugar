<?xml version="1.0" encoding="utf-8"?>
<com.sugar.demo.ui.widget.SignleTouchMotionLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_motionlayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layoutDescription="@xml/bottom_scene"
    app:layout_collapseMode="parallax">

    <View
        android:id="@+id/black_backgroundView"
        android:layout_width="match_parent"
        android:fitsSystemWindows="true"
        android:layout_height="match_parent"
         />

    <RelativeLayout
        android:id="@+id/sugar_title"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:background="@color/colorPrimary"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Sugar"
            android:textColor="@color/colorWhite"
            android:textStyle="bold"
            android:textSize="17sp" />
    </RelativeLayout>

    <include
        layout="@layout/content_layout"
        app:layout_constraintBottom_toBottomOf="parent" />

</com.sugar.demo.ui.widget.SignleTouchMotionLayout>
