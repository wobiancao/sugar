<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sugar.demo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".core.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/Appwelcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".ui.MainActivity" />-->
        <activity android:name=".ui.GankActivity" />
        <activity android:name=".ui.WanActivity" />
        <activity android:name=".ui.kt.KtGankActivity" />
        <activity android:name=".ui.kt.KtContainerActivity" />
        <activity android:name=".ui.kt.anko.GankAnkoActivity" />
        <activity android:name=".ui.kt.image.ImageLoadActivity" />
    </application>

</manifest>